import { Converter, InterfaceCoordinates } from "proj4";
import { Projection } from "./internal";
export declare class ProjectionTransform {
    from: Projection;
    to: Projection;
    converter: Converter;
    _transform: Function;
    constructor(from: Projection, to: Projection);
    /**
     * Transform a x and y location
     * @param x x coordinate
     * @param y y coordinate
     * @return transformed coordinates as [x, y]
     */
    transform(x: number, y: number): Array<number>;
    /**
     * Transform a coordinates array
     * @param array coordinate
     * @return transformed coordinates as [x, y]
     */
    transformCoordinateArray(array: Array<number>): Array<number>;
    /**
     * Transform InterfaceCoordinates
     * @return transformed coordinates as {x, y}
     * @param coordinate
     */
    transformCoordinate(coordinate: InterfaceCoordinates): InterfaceCoordinates;
    /**
     * Transform the coordinate bounds
     *
     * @param minX min x
     * @param minY min y
     * @param maxX max x
     * @param maxY max y
     * @return transformed coordinate bounds as [minX, minY, maxX, maxY]
     */
    transformBounds(minX: number, minY: number, maxX: number, maxY: number): Array<number>;
    /**
     * Get the from projection in the transform
     * @return from projection
     */
    getFromProjection(): Projection;
    /**
     * Get the to projection in the transform
     * @return to projection
     */
    getToProjection(): Projection;
    /**
     * Get the transform
     * @return transform
     */
    getConverter(): Converter;
    getInverseTransformation(): ProjectionTransform;
}
