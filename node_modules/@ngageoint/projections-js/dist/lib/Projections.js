"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Projections = void 0;
var internal_1 = require("./internal");
var proj4_1 = __importDefault(require("proj4"));
/**
 * Retrieves the proj4 projection parameter string for an authority and coordinate code
 */
var Projections = /** @class */ (function () {
    function Projections() {
    }
    Projections.getProjectionForName = function (name) {
        var _a = Projections.parseAuthorityAndCode(name), authority = _a.authority, code = _a.code;
        return Projections.getProjection(authority, code);
    };
    /**
     * Set projections for an authority. They will only be stored in the cache.
     * @param authority
     * @param projections
     */
    Projections.setProjections = function (authority, projections) {
        if (authority === void 0) { authority = internal_1.ProjectionConstants.AUTHORITY_EPSG; }
        if (Projections.cachedProjections[authority] == null) {
            Projections.cachedProjections[authority] = {};
        }
        Object.keys(projections).map(function (code) {
            var name = authority + ':' + code;
            proj4_1.default.defs(code, projections[code]);
            var definition = proj4_1.default.defs(name);
            Projections.cachedProjections[authority][code] = new internal_1.Projection(authority, code, definition);
        });
    };
    /**
     * Set projections for an authority. They will only be stored in the cache.
     * @param authority
     * @param code
     * @param proj4String
     */
    Projections.setProjection = function (authority, code, proj4String) {
        if (authority === void 0) { authority = internal_1.ProjectionConstants.AUTHORITY_EPSG; }
        if (Projections.cachedProjections[authority] == null) {
            Projections.cachedProjections[authority] = {};
        }
        var name = authority + ':' + code;
        proj4_1.default.defs(name, proj4String);
        var definition = proj4_1.default.defs(name);
        Projections.cachedProjections[authority][code.toString()] = new internal_1.Projection(authority, code.toString(), definition);
    };
    /**
     * Get the proj4 projection string for the authority coordinate code
     *
     * @param authority coordinate authority
     * @param code coordinate code
     * @param proj4String proj4 string
     * @return proj4 projection
     */
    Projections.getProjection = function (authority, code, proj4String) {
        if (authority === void 0) { authority = internal_1.ProjectionConstants.AUTHORITY_EPSG; }
        if (proj4String === void 0) { proj4String = undefined; }
        var projection;
        if (Projections.cachedProjections[authority] == null) {
            Projections.cachedProjections[authority] = {};
        }
        projection = Projections.cachedProjections[authority][code];
        if (projection == null) {
            var stringDef = proj4String;
            if (stringDef == null) {
                var definitions = null;
                switch (authority) {
                    case internal_1.ProjectionConstants.AUTHORITY_EPSG:
                        definitions = require('./projections/epsg').epsg;
                        break;
                    case internal_1.ProjectionConstants.AUTHORITY_OGC:
                        definitions = require('./projections/ogc').ogc;
                        break;
                    case internal_1.ProjectionConstants.AUTHORITY_NONE:
                        definitions = require('./projections/none').none;
                        break;
                    case internal_1.ProjectionConstants.AUTHORITY_NSG:
                        definitions = require('./projections/nsg').nsg;
                        break;
                }
                if (definitions != null) {
                    stringDef = definitions[code.toString()];
                }
            }
            if (stringDef == null) {
                throw new internal_1.ProjectionException("Projection from authority: " + authority + ' with code: ' + code + ' not found.');
            }
            var name_1 = authority + ':' + code;
            proj4_1.default.defs(name_1, stringDef);
            var definition = proj4_1.default.defs(name_1);
            projection = new internal_1.Projection(authority, code.toString(), definition);
            Projections.cachedProjections[authority][code.toString()] = projection;
        }
        return projection;
    };
    /**
     * Clear cache for a given authority
     * @param authority
     */
    Projections.clearCache = function (authority) {
        if (authority === void 0) { authority = null; }
        if (authority != null) {
            Projections.cachedProjections[authority] = {};
        }
        else {
            Projections.clear();
        }
    };
    Projections.clear = function () {
        Projections.cachedProjections = {};
    };
    /**
     * Gets the code from this projection's name
     * @param name
     */
    Projections.getCode = function (name) {
        return Projections.parseAuthorityAndCode(name).code;
    };
    /**
     * Gets metersPerUnit value from the projection, if provided
     * @param name
     */
    Projections.getMetersPerUnit = function (name) {
        var metersPerUnit = null;
        var def = Projections.getProjectionForName(name).getDefinition();
        if (def != null) {
            metersPerUnit = def.to_meter;
        }
        return metersPerUnit;
    };
    /**
     * Gets the units of this projection, if available
     * @param name
     */
    Projections.getUnits = function (name) {
        var units = null;
        var def = Projections.getProjectionForName(name).getDefinition();
        if (def != null) {
            units = def.units;
        }
        if (def != null && units == null) {
            var _a = Projections.parseAuthorityAndCode(name), authority = _a.authority, code = _a.code;
            if (authority === internal_1.ProjectionConstants.AUTHORITY_EPSG && code === internal_1.ProjectionConstants.EPSG_WORLD_GEODETIC_SYSTEM.toString()) {
                units = 'degrees';
            }
            else if (authority === internal_1.ProjectionConstants.AUTHORITY_EPSG && code === internal_1.ProjectionConstants.EPSG_WEB_MERCATOR.toString()) {
                units = 'm';
            }
            else if (authority === internal_1.ProjectionConstants.AUTHORITY_OGC && code === internal_1.ProjectionConstants.OGC_CRS84.toString()) {
                units = 'degrees';
            }
            else if (def.projName === 'longlat') {
                units = 'degrees';
            }
        }
        return units;
    };
    /**
     * Gets the ProjectionDefinition for WebMercator
     * @return ProjectionDefinition
     */
    Projections.getWebMercatorProjection = function () {
        return Projections.getProjectionForName(internal_1.ProjectionConstants.AUTHORITY_EPSG + ":" + internal_1.ProjectionConstants.EPSG_WEB_MERCATOR);
    };
    /**
     * Gets the ProjectionDefinition for WebMercator
     * @return ProjectionDefinition
     */
    Projections.getCRS84Projection = function () {
        return Projections.getProjectionForName(internal_1.ProjectionConstants.AUTHORITY_OGC + ":" + internal_1.ProjectionConstants.OGC_CRS84);
    };
    /**
     * Gets the ProjectionDefinition for WGS84
     * @return ProjectionDefinition
     */
    Projections.getWGS84Projection = function () {
        return Projections.getProjectionForName(internal_1.ProjectionConstants.AUTHORITY_EPSG + ":" + internal_1.ProjectionConstants.EPSG_WORLD_GEODETIC_SYSTEM);
    };
    Projections.isWebMercator = function (proj) {
        if (typeof proj === 'string') {
            var _a = Projections.parseAuthorityAndCode(proj), authority = _a.authority, code = _a.code;
            return authority === internal_1.ProjectionConstants.AUTHORITY_EPSG && code === internal_1.ProjectionConstants.EPSG_WEB_MERCATOR.toString();
        }
        else {
            return Projections.projectionsEqual(proj, Projections.getWebMercatorProjection());
        }
    };
    Projections.isWGS84 = function (proj) {
        if (typeof proj === 'string') {
            var _a = Projections.parseAuthorityAndCode(proj), authority = _a.authority, code = _a.code;
            return authority === internal_1.ProjectionConstants.AUTHORITY_EPSG && code === internal_1.ProjectionConstants.EPSG_WORLD_GEODETIC_SYSTEM.toString();
        }
        else {
            return Projections.projectionsEqual(proj, Projections.getWGS84Projection());
        }
    };
    Projections.projectionsEqual = function (from, to) {
        return from.equalsProjection(to);
    };
    Projections.getProjectionTransformation = function (from, to) {
        var fromDef = typeof from === 'string' ? Projections.getProjectionForName(from) : from;
        var toDef = typeof to === 'string' ? Projections.getProjectionForName(to) : to;
        // @ts-ignore
        return new internal_1.ProjectionTransform(fromDef, toDef);
    };
    /**
     * Parses the authority and code from a name
     * @param name
     */
    Projections.parseAuthorityAndCode = function (name) {
        var authority = null;
        var code = null;
        if (name === "CRS84") {
            authority = internal_1.ProjectionConstants.AUTHORITY_OGC;
            code = name;
            // OGC URN handling
        }
        else if (name.toLowerCase().startsWith("urn:ogc:def:crs")) {
            var parts = name.split(":");
            if (parts.length === 7) {
                authority = parts[4];
                code = parts[6];
            }
        }
        else {
            var projectionParts = name.split(":");
            switch (projectionParts.length) {
                case 1:
                    authority = internal_1.ProjectionConstants.AUTHORITY_EPSG;
                    code = projectionParts[0];
                    break;
                case 2:
                    authority = projectionParts[0];
                    code = projectionParts[1];
                    break;
                default:
                    throw new internal_1.ProjectionException("Invalid projection name '" + name + "', expected 'authority:code' or 'epsg_code'");
            }
        }
        return {
            authority: authority,
            code: code
        };
    };
    /**
     * Projections property file name prefix
     */
    Projections.PROJECTIONS_PROPERTY_FILE_PREFIX = "projections";
    /**
     * Projections property file name suffix
     */
    Projections.PROJECTIONS_PROPERTY_FILE_SUFFIX = "properties";
    /**
     * Properties for each authority
     */
    Projections.cachedProjections = {};
    return Projections;
}());
exports.Projections = Projections;
//# sourceMappingURL=Projections.js.map