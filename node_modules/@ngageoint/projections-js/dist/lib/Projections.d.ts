import { Projection, ProjectionTransform } from "./internal";
/**
 * Retrieves the proj4 projection parameter string for an authority and coordinate code
 */
export declare class Projections {
    /**
     * Projections property file name prefix
     */
    static readonly PROJECTIONS_PROPERTY_FILE_PREFIX = "projections";
    /**
     * Projections property file name suffix
     */
    static readonly PROJECTIONS_PROPERTY_FILE_SUFFIX = "properties";
    /**
     * Properties for each authority
     */
    private static cachedProjections;
    static getProjectionForName(name: string): Projection;
    /**
     * Set projections for an authority. They will only be stored in the cache.
     * @param authority
     * @param projections
     */
    static setProjections(authority: string, projections: any): void;
    /**
     * Set projections for an authority. They will only be stored in the cache.
     * @param authority
     * @param code
     * @param proj4String
     */
    static setProjection(authority: string, code: string | number, proj4String: string): void;
    /**
     * Get the proj4 projection string for the authority coordinate code
     *
     * @param authority coordinate authority
     * @param code coordinate code
     * @param proj4String proj4 string
     * @return proj4 projection
     */
    static getProjection(authority: string, code: string | number, proj4String?: string): Projection;
    /**
     * Clear cache for a given authority
     * @param authority
     */
    static clearCache(authority?: string): void;
    static clear(): void;
    /**
     * Gets the code from this projection's name
     * @param name
     */
    static getCode(name: any): any;
    /**
     * Gets metersPerUnit value from the projection, if provided
     * @param name
     */
    static getMetersPerUnit(name: any): any;
    /**
     * Gets the units of this projection, if available
     * @param name
     */
    static getUnits(name: any): any;
    /**
     * Gets the ProjectionDefinition for WebMercator
     * @return ProjectionDefinition
     */
    static getWebMercatorProjection(): Projection;
    /**
     * Gets the ProjectionDefinition for WebMercator
     * @return ProjectionDefinition
     */
    static getCRS84Projection(): Projection;
    /**
     * Gets the ProjectionDefinition for WGS84
     * @return ProjectionDefinition
     */
    static getWGS84Projection(): Projection;
    static isWebMercator(proj: string | Projection): boolean;
    static isWGS84(proj: string | Projection): boolean;
    static projectionsEqual(from: Projection, to: Projection): boolean;
    static getProjectionTransformation(from: string | Projection, to: string | Projection): ProjectionTransform;
    /**
     * Parses the authority and code from a name
     * @param name
     */
    static parseAuthorityAndCode(name: string): {
        authority: any;
        code: any;
    };
}
