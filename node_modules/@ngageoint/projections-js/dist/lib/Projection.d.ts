import { ProjectionDefinition } from "proj4";
import { ProjectionTransform } from "./internal";
/**
 * Single Projection for an authority and code
 */
export declare class Projection {
    /**
     * Projection authority
     */
    private readonly authority;
    /**
     * Coordinate code
     */
    private readonly code;
    /**
     * Definition
     */
    private readonly definition;
    /**
     * Constructor
     *
     * @param authority
     *            coordinate authority
     * @param code
     *            coordinate code
     * @param definition
     *            proj4 definition
     */
    constructor(authority: string, code: string, definition: ProjectionDefinition);
    /**
     * Get the coordinate authority
     *
     * @return authority
     */
    getAuthority(): string;
    /**
     * Get the coordinate code
     *
     * @return code
     */
    getCode(): string;
    /**
     * Get the projection definition
     * @return definition
     */
    getDefinition(): ProjectionDefinition;
    /**
     * Get the transformation from this Projection to the EPSG code. Each thread
     * of execution should have it's own transformation.
     *
     * @param projection projection
     * @return transform
     */
    getTransformation(projection: Projection): ProjectionTransform;
    /**
     * Get the inverse transformation from this Projection to the EPSG code. Each thread
     * of execution should have it's own transformation.
     *
     * @param projection projection
     * @return transform
     */
    getInverseTransformation(projection: Projection): ProjectionTransform;
    /**
     * Get the transformation from this Projection to the EPSG code. Each thread
     * of execution should have it's own transformation.
     *
     *
     * @param authority
     *            coordinate authority
     * @param code
     *            coordinate code
     * @return transform
     */
    getTransformationTo(authority: string, code: string | number): ProjectionTransform;
    /**
     * Get the inverse transformation from this Projection to the EPSG code. Each thread
     * of execution should have it's own transformation.
     *
     *
     * @param authority
     *            coordinate authority
     * @param code
     *            coordinate code
     * @return transform
     */
    getInverseTransformationTo(authority: string, code: string | number): ProjectionTransform;
    /**
     * Check if this projection is equal to the authority and code
     * @param authority coordinate authority
     * @param code coordinate code
     * @return true if equal
     */
    equals(authority: string, code: string): boolean;
    /**
     * Check if this projection is equal to the authority and code
     * @param authority coordinate authority
     * @param code coordinate code
     * @return true if equal
     */
    equalsProjection(other: Projection): boolean;
    /**
     * {@inheritDoc}
     */
    toString(): string;
}
